# 领域上下文

## 文档说明

- 目标：沉淀 MealMate 的业务本质、领域边界与统一语言，作为后续设计、计划与研发实现的业务基线。
- 当前状态：本文件已成为领域上下文的唯一主入口。

## 业务本质

MealMate 的核心不是“菜谱展示”，而是围绕家庭饮食决策构建的家庭饮食规划与执行系统。系统要同时覆盖家庭成员差异化饮食约束、计划生成、执行准备、实际记录与营养复盘，形成可持续优化的业务闭环。

## 业务闭环

1. 建立家庭成员画像与饮食约束。
2. 管理菜品知识库与标签体系。
3. 生成一周用餐计划。
4. 生成采购清单与备菜计划。
5. 记录实际饮食情况。
6. 对比计划与实际，输出营养分析与优化建议。

## 核心业务能力

| 业务能力 | 输入 | 输出 | 关键规则 |
| --- | --- | --- | --- |
| 家庭画像管理 | 成员信息、口味偏好、忌口、权限 | 家庭饮食画像 | 成员差异化约束必须可表达 |
| 菜品管理 | 菜名、食材、标签、适配人群、营养信息 | 标准化菜品库 | 菜品可标签化、可筛选、可扩展 |
| 周计划生成 | 家庭画像、菜品库、季节信息、历史记录 | 7 天三餐计划 | 不重样、减脂分布、宝宝适配、口味平衡 |
| 备菜与采购 | 周计划、配方、库存或保存规则 | 采购清单、备菜计划 | 按餐次聚合、按食材归并、可保存 |
| 实际记录 | 照片、备注、用餐时间、关联计划 | 实际饮食记录 | 计划与实际可追踪 |
| 营养分析 | 计划数据、实际饮食、营养规则 | 分析报告、优化建议 | 宝宝营养、减脂目标、均衡性评估 |
| 提醒与推送 | 计划、时间配置、任务调度 | 备菜推送、采购提醒 | 固定时点、可关闭、可回溯 |

## 业务阶段

- 配置阶段：维护家庭成员、口味偏好、忌口、权限和菜品库。
- 规划阶段：生成周计划、筛选菜品、手动调整和补充自定义食谱。
- 执行阶段：采购、备菜、做饭、拍照记录和备注补充。
- 复盘阶段：对比计划与实际、分析营养结果、复用历史经验并优化策略。

## 核心领域与限界上下文

| 上下文 | 职责 | 定位 |
| --- | --- | --- |
| Family Context | 家庭成员、偏好、忌口、权限、画像配置 | 核心域 |
| Recipe Context | 菜品、配方、标签、营养信息、分类 | 核心域 |
| MealPlan Context | 周计划、餐次安排、生成规则、重复校验 | 核心域 |
| Preparation Context | 采购清单、备菜计划、保存方式 | 重要支撑域 |
| Record Context | 照片记录、备注、计划关联、差异比对 | 重要支撑域 |
| Nutrition Context | 营养报告、达标校验、建议输出 | 重要支撑域 |
| Recommendation Context | 季节推荐、应急食材、野菜活动建议 | 一般支撑域 |
| Notification Context | 提醒配置、定时任务、消息投递 | 通用支撑域 |

## 建议聚合

| 聚合 | 业务职责 | 关键规则 |
| --- | --- | --- |
| `FamilyProfile` | 管理家庭整体饮食规则与成员画像 | 宝宝必须绑定幼儿饮食约束；仅有权限角色可改计划 |
| `Recipe` | 沉淀标准菜品能力 | 菜品必须具备类型与适配人群；宝宝菜和减脂菜要有专属约束字段 |
| `WeeklyMealPlan` | 管理一周饮食计划与调整 | 一周默认不重复；减脂餐按周目标均匀分布；宝宝餐必须通过适配校验 |
| `PrepPlan` | 派生备菜与采购执行方案 | 相同食材归并统计；宝宝专属与成人辛辣食材分组展示 |
| `MealRecord` | 记录实际食用情况 | 每条记录必须绑定日期与餐次；允许计划内关联与独立补录 |
| `NutritionReport` | 输出周度分析结果 | 分析结论必须能追溯到计划或实际记录数据 |

## 统一语言

- 家庭成员：系统中的饮食参与者，如妻子、丈夫、宝宝。
- 饮食画像：成员口味偏好、忌口、饮食目标和限制条件的集合。
- 菜品：可被计划、推荐、记录的标准业务对象。
- 餐次：早餐、午餐、晚餐。
- 周计划：覆盖 7 天三餐的结构化用餐安排。
- 减脂餐：满足低脂、低卡、高纤维规则的特定菜品安排。
- 宝宝适配：菜品满足软烂、少盐少糖、无辛辣等幼儿约束。
- 备菜计划：面向执行层的食材预处理、保存和烹饪说明。
- 采购清单：统一使用 `ShoppingList`，禁用 `PurchaseList`。
- 实际饮食记录：用户真实食用情况的照片与备注记录。
- 营养分析：对饮食结构进行规则化评估的结果。
- 提醒任务：统一使用 `NotifyTask`，禁用 `Reminder`、`Task`。

## 领域服务与领域事件建议

### 领域服务

- `WeekPlanGenerateDomainService`
- `RecipeMatchDomainService`
- `DuplicateCheckDomainService`
- `NutritionEvaluateDomainService`
- `PrepPlanGenerateDomainService`
- `SeasonalRecommendDomainService`
- `WildVegetableGuideDomainService`

### 领域事件

- `WeeklyMealPlanGenerated`
- `WeeklyMealPlanAdjusted`
- `ManualRecipeAdded`
- `PrepPlanGenerated`
- `MealRecordCreated`
- `WeeklyNutritionReportGenerated`
- `ReminderTriggered`

## 与 COLA 分层的衔接

- `adapter` 只负责协议输入输出与参数校验，不表达业务规则。
- `app` 负责用例编排、事务边界和命令查询执行。
- `domain` 承载上述聚合、领域服务、统一语言和核心规则。
- `infrastructure` 实现仓储、网关、持久化与任务调度，不反向污染领域模型。
