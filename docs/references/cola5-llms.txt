MealMate AI 参考摘要：COLA 5.0

1. 分层职责必须清晰，adapter 不写业务规则，domain 不依赖技术实现。
2. 仓储接口属于 domain，实现属于 infrastructure。
3. App 层负责事务、编排和用例执行，不替代领域建模。
4. 当出现跨层偷依赖的冲动时，优先回到领域边界重新建模。
5. 新功能的第一问题不是“代码放哪”，而是“这条业务规则属于哪一层”。

