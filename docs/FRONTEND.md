# 前后端协作边界

MealMate 当前仓库以后台服务为主，但仍需对 Web 或未来 App 接入保持清晰边界。

## 原则

- 前端页面结构不应反向驱动领域模型设计。
- 后端接口按业务域聚合，而不是按页面组件拆分。
- 与拍照、裁剪、上传相关的能力，应明确区分前端处理和后端存储边界。

## 当前约束

- Web 端可调起摄像头或相册选图。
- 裁剪信息以 JSON 形式进入记录相关存储字段。
- 接口需要兼容未来 App 与微前端接入，但不为此提前引入复杂抽象。

