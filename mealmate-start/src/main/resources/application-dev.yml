spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://8.xxx.xxx.8:3306/mealmate
    username: xxx
    password: xxx

logging:
  level:
    io.yggdrasil.labs.mealmate.*: debug
    root: info

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

# 开发环境特定配置
# 例如：数据库连接、Redis 等
